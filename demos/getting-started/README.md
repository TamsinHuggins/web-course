# Getting Started with HTML, CSS and JavaScript (Demos)

Open these `.html` files directly in a browser.

## Demos

- `01-html-structure.html` — basic document structure + semantic tags
- `02-css-inline.html` — inline CSS styles
- `03-css-style-tag.html` — using a `<style>` block + selectors
- `04-css-external.html` — external stylesheet (`styles.css`)
- `05-js-inline-vs-defer.html` — JavaScript in `<head>` vs `defer` + functions
- `06-selector-api.html` — querySelector/querySelectorAll + `:checked`
